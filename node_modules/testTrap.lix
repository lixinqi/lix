#!/usr/local/bin/node

f := [cb]->{
	cc call [break]->{
		trap call []->{
			'catch' print
			break call
			'--trap return--'
		}
		cb call
		break call
	}
}

f call []->{
	'nice after' print
	trap call | call | print
	'nice after' print
	'good' call
}

'end' print
