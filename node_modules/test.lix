test := [name cb]->{
	getCounter := $counter.generator call
	t := [cond]->{
		counter := getCounter call
		if (cond == false) {
			['Failed! ' name ' #' counter] .Join ''  | print
		} else {
			['OK ' name ' #' counter] .Join '' | print
		}
	}

	cb call t
}

test.subTest := [t name cb]->{
}

test
