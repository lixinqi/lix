#!/usr/local/bin/node

[xl end]->{
	cont := null
	[]->{
		cc call [brk]->{
			if (cont) {
				brk cont
			} else {
				_brk := brk
				xl | map [i]->{
							_brk = cc call [next]->{
								cont = next
								i _brk
							}
						} 
					| exec
				end brk
			}
		}
	}
}
