[arr cb]->{
	i := 0
	if (arr $instanceof Array) {
		while (i < arr.length) {
			cb call arr.(i) i
			i = i + 1
		}
	} (arr $instanceof Object) {
		keys := arr Object.Keys
		while (i < keys.length) {
			cb call arr.(keys.(i)) keys.(i)
			i = i + 1
		}
	}
}
