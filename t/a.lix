#!/usr/local/bin/node
map := (arr cb)->{
	ret := []
	arr foreach (item i)->{
		ret.[i] = item cb i
	}
	ret
}

square := (i)->{i * i}

compose := (f0 f1)->{
	(i)->{i | f0 | f1}
}

3 (square compose square) | print


[1 2 3] map (square compose square) | print

test := ()->{
	[1 2 3]
}

test0 := ()->{
	{
		.1 	0
		.2 	1
		.3	2
	}
}

test1 := ()->{
	.method
}

test2 := ()->{
	./good
}

[test test0 test1 test2] foreach (i)->{
	i | call | print
}

