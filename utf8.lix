#!/usr/local/bin/node ./lix.js
问候语 := "你好"

问候语 | print 

打印到终端 := print

再 := (f0 f1)->{
	(i)->{ i | f0 | f1 }
}

平方 := (i)->{
	i * i
}

四次方 := 平方 再 平方

2 四次方 | 打印到终端

2 (平方 再 平方) | 打印到终端

2 | 平方 | 平方 | 打印到终端

对象o := {
	.名字 ()->{
		"李新奇"
	}
}

人物p := 对象o

人物p | .名字 | 打印到终端

