#!/usr/bin/env lix

./node_modules/a.js require | print
$[./node_modules/a.js] print

$a
$a.name
$a.name | print
$a | print
$a .hi

inc1 := + $_.Partial 1
inc2 := + $_.Partial 2
inc3 := inc1 $_.Compose inc2

1 inc3 | print

# /tmp/a.txt | $fs.ReadFileSync | print
# /tmp/a.txt | $fs.ReadFileSync | $php.explode "\n" |
# 	foreach (line index)->{
# 		[(index + 1) ' ' line] .Join '' | print	
# 	}
